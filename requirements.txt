# ⚠️ torch ставится ОТДЕЛЬНО и строго под cu121:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
# Причина: GTX 1080 — это Pascal, объявлен устаревшим в CUDA 12.8 и убран в 13.
# Сразу после установки проверить:
#   python -c "import torch; print(torch.cuda.get_arch_list())"  -> нужен sm_61

faster-whisper>=1.1.0
pyannote.audio>=4.0.0
ffmpeg-python>=0.2.0
fastapi>=0.115.0
uvicorn>=0.32.0
python-multipart>=0.0.9
gradio>=5.0.0
python-dotenv>=1.0.0
jiwer>=3.0.0
